# Theme Rotation Tracker — public dashboard

This repository contains only the sanitized, read-only website generated by the private research tracker. It deliberately excludes credentials, source archives, screenshots, raw research files and the private calculation pipeline.

Open the dashboard: https://pcheong22.github.io/theme-rotation-tracker-site/

The private tracker refreshes `docs/` after its scheduled daily market-data run. GitHub Pages publishes that folder automatically.
Public read-only dashboard for the Theme Rotation Tracker
